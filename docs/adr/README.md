# Architecture Decision Records

Accepted ADRs are immutable historical records. A changed decision receives a new ADR that supersedes the old one.

Use `0000-template.md` for every fundamental decision.
